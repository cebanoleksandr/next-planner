import { SubGoal } from "@/utils/interfaces";
import { FC } from "react";

interface IProps {
  data: SubGoal;
}

const SubgoalCard: FC<IProps> = ({ data }) => {
  return (
    <div></div>
  )
}

export default SubgoalCard;
