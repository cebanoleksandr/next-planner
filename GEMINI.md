# GEMINI.md: AI-Generated Project Context

This file is generated by an AI to provide context for future interactions.

## Project Overview

This is a [Next.js](https://nextjs.org/) project called "Axis". Based on the file structure and dependencies, the goal of this project appears to be building a web application that helps users set and manage their aims.

The project is in its early stages of development, with a basic structure and some placeholder pages.

**Key Technologies:**

*   **Framework:** Next.js (with App Router)
*   **Language:** TypeScript
*   **UI:** React
*   **Styling:** Tailwind CSS
*   **State Management:** Redux Toolkit
*   **Data Fetching:** React Query (TanStack Query)
*   **Forms:** Formik
*   **Authentication:** Keycloak
*   **Internationalization:** next-intl
*   **Linting:** ESLint

**Architecture:**

The project follows the standard Next.js App Router structure.

*   The `app` directory contains the main application code, with a `[locale]` directory for internationalization.
*   The `components` directory likely holds reusable React components.
*   The `utils` directory probably contains utility functions.
*   Configuration files like `next.config.ts`, `tailwind.config.ts`, and `tsconfig.json` define the project's behavior.

## Building and Running

You can use the following npm scripts (from `package.json`) to work with this project:

*   **Run the development server:**
    ```bash
    npm run dev
    ```
    This will start the application on [http://localhost:3000](http://localhost:3000).

*   **Build the project for production:**
    ```bash
    npm run build
    ```

*   **Start the production server:**
    ```bash
    npm run start
    ```

*   **Run the linter:**
    ```bash
    npm run lint
    ```

## Development Conventions

*   **TypeScript:** The project is written in TypeScript, so all new code should be strongly typed.
*   **ESLint:** ESLint is used for code quality and consistency. Run `npm run lint` to check for issues.
*   **App Router:** The project uses the Next.js App Router, so new pages and layouts should be created within the `app` directory.
*   **Internationalization:** The `next-intl` library is used for internationalization. Text should be added to the appropriate message files.
*   **Styling:** The project uses Tailwind CSS for styling. Utility classes should be used whenever possible.
*   **State Management:** Redux Toolkit is used for global state management, while React Query is used for server state.
